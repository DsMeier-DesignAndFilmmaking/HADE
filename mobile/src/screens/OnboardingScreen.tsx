import React from "react";
import { StyleSheet, View } from "react-native";

/**
 * First-time onboarding — location permission, contact import, intent calibration.
 */
export default function OnboardingScreen(): React.JSX.Element {
  // TODO: Step 1 — Location permission with warm explanation
  // TODO: Step 2 — Contact import (optional)
  // TODO: Step 3 — Intent calibration
  return <View style={styles.container} />;
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#0D0D0D",
  },
});
